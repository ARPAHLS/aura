# Guarded tools

Token limit, tool allowlist, and human confirm before `gmail.send`.

```bash
pip install -e ..
python main.py
```

Shows: constraint engine, `ApprovalRequired`, audit events for violations and approvals.
