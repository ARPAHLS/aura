# Minimal loop

Wraps a two-event loop under AURA. Creates agent `minimal-demo` on first run.

```bash
pip install -e ..
python main.py
```

Shows: auto agent registration, `AURA-000n`, JSONL + summary export under `~/.aura/sessions/`.
