# Task mode

Session runs in `task` mode and closes after `complete_goal()`.

```bash
pip install -e ..
python main.py
```

Shows: purpose on agent profile, task events, conformance summary on close.
