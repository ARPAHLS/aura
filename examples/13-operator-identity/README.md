# Example 13 — Operator identity

Optional verified operator trailer on session open (mock adapter stands in for OIDC/Auth0 in CI).

```bash
python examples/13-operator-identity/main.py
```

For spectrum `high` / `full`, verified identity is required by default unless `spectrum.identity_required: false`.

→ [integrations/identity/README.md](../../integrations/identity/README.md) · [trust-paths.md](../../docs/trust-paths.md)
